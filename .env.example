# Copy this file to .env on the server and fill in your values.
# Do not commit .env (it is in .gitignore).

# Site
SITE_BASE_URL=https://vanillakiller.com
PORT=3000

# Optional: extra CORS origins (comma-separated)
# ALLOWED_ORIGINS=

# Mailchimp (all forms: contact + lead magnets)
# Get API key: Mailchimp → Account → Extras → API keys. Server prefix is in the key (e.g. us19).
# Audience ID: Audience → Settings → Audience name and defaults → Audience ID.
MAILCHIMP_API_KEY=
MAILCHIMP_SERVER_PREFIX=us19
MAILCHIMP_AUDIENCE_ID=
# Tags used: "submitted website contact form" (book-a-call, website-review); lead magnets use tags from settings.js (e.g. lead-50things).

# Notion VKCRM (get token from notion.so/my-integrations; connect integration to VKCRM database)
NOTION_TOKEN=
NOTION_DATABASE_ID=
# Optional: page ID for the website wiki. Create a page in Notion, share with integration, copy ID from URL. Then: node scripts/sync-wiki-to-notion.js
# NOTION_WIKI_PAGE_ID=

# Optional: if set, booking-confirmed webhook requires this secret in Authorization header
# BOOKING_WEBHOOK_SECRET=
