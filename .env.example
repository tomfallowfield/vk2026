# Copy this file to .env on the server and fill in your values.
# Do not commit .env (it is in .gitignore).

# Site
SITE_BASE_URL=https://vanillakiller.com
PORT=3000

# Optional: extra CORS origins (comma-separated)
# ALLOWED_ORIGINS=

# Mailchimp (all forms: contact + lead magnets)
# Get API key: Mailchimp → Account → Extras → API keys. Server prefix is in the key (e.g. us19).
# Audience ID: Audience → Settings → Audience name and defaults → Audience ID.
MAILCHIMP_API_KEY=
MAILCHIMP_SERVER_PREFIX=us19
MAILCHIMP_AUDIENCE_ID=
# Tags used: "submitted website contact form" (book-a-call, website-review); lead magnets use tags from settings.js (e.g. lead-50things).

# Notion VKCRM (get token from notion.so/my-integrations; connect integration to VKCRM database)
NOTION_TOKEN=
NOTION_DATABASE_ID=
# Optional: page ID for the website wiki. Create a page in Notion, share with integration, copy ID from URL. Then: node scripts/sync-wiki-to-notion.js
# NOTION_WIKI_PAGE_ID=

# Optional: if set, booking-confirmed webhook requires this secret in Authorization header
# BOOKING_WEBHOOK_SECRET=

# Deploy webhook: if set, POST /vk2026/api/webhooks/deploy requires X-Deploy-Secret header (or ?secret=) to match. Used by GitHub Actions auto-deploy.
# DEPLOY_WEBHOOK_SECRET=

# Optional: if set, GET /vk2026/api/analytics/events (events viewer) requires ?view_key= to match. Leave unset for open access on same server.
# DEMO_VIEW_KEY=

# MySQL (analytics: visitors, events). Create DB and run server/db/schema.sql, then server/db/migrations/001_add_return_visit_notified_at.sql
# DB_HOST=localhost
# DB_USER=
# DB_PASSWORD=
# DB_NAME=vk2026_analytics

# Return-visit notifications: when someone with an email (from a form) views the site again, send email + Slack + add to Notion (once per hour per visitor)
# NOTIFICATION_EMAIL_TO=you@example.com
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/...
# NOTION_RETURN_VISITS_DATABASE_ID=   (create a DB in Notion with columns: Name (title), Email, Visitor ID, Returned at (date); share with integration)
# SMTP (for email): e.g. Gmail app password
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=
